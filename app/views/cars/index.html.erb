<h1>Browse our cars</h1>
<p>You are only one step away to rent a car</p>

<ul>
  <% @cars.each do |car| %>
    <li><%= link_to "#{car.make}, #{car.model} - $#{car.price_day}", car_path(car) %></li>
  <% end %>
</ul>
